package org.acme.insurance;

import java.lang.Number;

rule "TooManyAccidents"
	dialect "mvel"
	when
		Driver( numberOfAccidents >= 4 )
	then
		Rejection fact0 = new Rejection();
		fact0.setReason( "Then reject with reason Too Many Accidents" );
		insert( fact0 );
end
